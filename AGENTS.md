# AGENTS.md

**The single source of truth for AI coding agents in this repository is [`CLAUDE.md`](./CLAUDE.md).**

This file previously held Grok-specific instructions; that content has been merged into `CLAUDE.md`, which now applies to **all** AI coding agents (Claude Code and others) — not just Claude.

👉 **Read [`CLAUDE.md`](./CLAUDE.md).** It covers:

- Project overview & current state (Phase 16+)
- Tech stack (verified versions) and build/test commands
- **Development workflow & standards** — issue-driven chunks, 12-Factor alignment, Laws of UX
- Architecture: event pipeline, key services + decomposition hotspots, API routes & response conventions
- Database models, environment variables, SSL/HTTPS
- Security & privacy baseline, code-health rules
- Production access & deployment, BMAD workflows, Playwright UI testing
- Standing improvement backlog
