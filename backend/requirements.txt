# FastAPI and Web Framework
fastapi[standard]==0.115.0
uvicorn[standard]==0.24.0
python-multipart>=0.0.6

# Task Scheduling
apscheduler>=3.10.4

# Retry Logic (Phase 3)
tenacity>=8.2.0

# Database
sqlalchemy>=2.0.36
alembic>=1.14.0

# Data Validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Camera and Image Processing
opencv-python-headless>=4.12.0  # Headless version for servers (no GUI/libGL dependency)
av>=12.0.0  # PyAV for secure RTSP (rtsps://) streams

# Security and Encryption
cryptography==44.0.1
python-jose[cryptography]==3.5.0
bcrypt>=4.2.0
slowapi>=0.1.9

# AI Vision APIs
openai>=1.54.0
anthropic>=0.39.0
google-generativeai>=0.8.0
pillow>=10.0.0

# UniFi Protect Integration (Phase 2)
uiprotect>=6.0.0  # UniFi Protect API client

# Web Push Notifications (Phase 4)
pywebpush>=2.0.0  # Web Push with VAPID authentication

# Email Delivery (Phase 4 - Story P4-4.3)
aiosmtplib>=3.0.0  # Async SMTP client for digest email delivery

# MQTT Integration (Phase 4 - Home Assistant)
paho-mqtt>=2.0.0  # MQTT client for Home Assistant integration

# HomeKit Integration (Phase 4 - Story P4-6.1)
HAP-python>=4.9.0  # HomeKit Accessory Protocol for Apple Home integration
qrcode>=7.4.0  # QR code generation for HomeKit pairing

# Temporal Context Engine (Phase 4 - Story P4-3.1)
sentence-transformers>=2.2.0  # CLIP model for image embeddings

# ONVIF Camera Discovery (Phase 5 - Story P5-2.1)
WSDiscovery>=2.0.0  # WS-Discovery protocol for camera auto-discovery
onvif-zeep>=0.2.12  # ONVIF SOAP client for camera capabilities

# Logging and Monitoring
python-json-logger>=2.0.0
prometheus_client>=0.19.0
psutil>=5.9.0

# APNS Push Notifications (Phase 11 - Story P11-2.1)
httpx[http2]>=0.26.0  # HTTP/2 client for APNS
PyJWT>=2.8.0  # JWT generation for APNS authentication

# FCM Push Notifications (Phase 11 - Story P11-2.2)
firebase-admin>=6.4.0  # Firebase Admin SDK for FCM

# Development and Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
